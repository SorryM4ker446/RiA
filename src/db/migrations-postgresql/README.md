# PostgreSQL migration archive

These migrations preserve the database history from the hosted Web application. They are not applied by the local SQLite runtime.

The active SQLite migrations live in `src/db/migrations`.
