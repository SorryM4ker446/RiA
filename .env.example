# Local development stores SQLite data under .desktop-data/dev by default.
# Override with another path relative to the repository root when needed.
LOCAL_DATABASE_FILE=".desktop-data/dev/app.db"
# The shared app client enforces one SQLite connection, a 30-second acquisition
# wait (queries and transactions), and a 5-second external-lock timeout.
# These connection settings are automatic; do not append them to this file path.
# Media defaults to a media/ directory next to the SQLite file.
# Optional absolute path for local Web deployments; Electron sets its own userData path.
# MEDIA_DIRECTORY="D:/private-ai-data/media"
# Optional absolute directory holding old generated-videos files for import on history reads.
# LEGACY_VIDEO_DIRECTORY="D:/private-ai-data/legacy-videos"
# Runtime mode: web, desktop, or test. Desktop launch scripts set this automatically.
# APP_RUNTIME="web"
# Optional exact browser origin (no trailing slash). Unset permits loopback hosts only.
# Required for a non-loopback Web host; your reverse proxy must preserve that Host.
# APP_ORIGIN="https://assistant.example.invalid"
# Desktop ignores APP_ORIGIN and uses its generated loopback host and Cookie.
OPENROUTER_API_KEY=""
TAVILY_API_KEY=""
# Optional: embedding model used for semantic memory/knowledge retrieval.
# EMBEDDING_MODEL_ID="openai/text-embedding-3-small"
# Optional: disable authentication (single shared demo user). Enabled by local scripts and E2E.
# Keep this off for any network-accessible authenticated Web service.
# AUTH_DISABLED="1"
# Optional: override Tavily endpoint for local E2E/mocks
# TAVILY_SEARCH_URL="http://127.0.0.1:4010/search"
# Optional OpenRouter headers (recommended by OpenRouter)
# OPENROUTER_HTTP_REFERER="http://localhost:3000"
# OPENROUTER_SITE_NAME="Private AI Assistant"
# Optional: force server-side outbound requests through proxy
# OUTBOUND_PROXY_URL="http://127.0.0.1:7897"
# NO_PROXY="localhost,127.0.0.1,::1"

# Desktop session variables are generated at runtime. Do not persist them in .env.
# DESKTOP_SESSION_TOKEN=""
# DESKTOP_SERVER_HOST="127.0.0.1:3110"
